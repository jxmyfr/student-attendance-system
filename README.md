# 🎓 Student Attendance System (Face Recognition)

ระบบเช็กชื่อนักเรียนอัตโนมัติด้วยเทคโนโลยีจดจำใบหน้า (Face Recognition) พัฒนาขึ้นเพื่อแก้ปัญหาการเช็กชื่อแบบเดิมที่ใช้เวลานานและอาจเกิดความคลาดเคลื่อน โดยระบบสามารถตรวจจับใบหน้าแบบ Real-time ผ่านกล้องเว็บแคมหรือกล้องมือถือ และบันทึกข้อมูลลงฐานข้อมูลอัตโนมัติ

## ✨ ฟีเจอร์หลัก (Features)

- **Real-time Face Recognition:** สแกนและระบุตัวตนนักเรียนได้อย่างรวดเร็ว (รองรับ HOG/CNN Model)
- **Web Dashboard:** หน้าต่างแสดงผลผ่าน Web Browser (ใช้งานง่ายผ่านมือถือและคอมพิวเตอร์)
- **Automated Database:** บันทึกเวลาเข้าเรียน สาย ขาด ลงฐานข้อมูล SQLite อัตโนมัติ
- **Student Management:** มีระบบเพิ่ม (Add) และ ลบ (Delete) ข้อมูลใบหน้านักเรียนได้ง่ายๆ ผ่าน Script
- **Thai Language Support:** รองรับการแสดงผลชื่อนักเรียนเป็นภาษาไทยบนหน้าจอกล้อง

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **Backend:** Python (Flask)
- **Computer Vision:** OpenCV, `face_recognition` (dlib)
- **Database:** SQLite (SQLAlchemy)
- **Frontend:** HTML, CSS

## 🚀 วิธีการติดตั้งและใช้งาน (Installation & Usage)

**1. Clone โปรเจคลงเครื่อง**

```bash
git clone [https://github.com/jxmyfr/student-attendance-system.git](https://github.com/jxmyfr/student-attendance-system.git)
cd student-attendance-system

1. ระบบหลังบ้านและการจัดการข้อมูล (Backend & Logic)
ระบบเรียนรู้ใบหน้าอัตโนมัติ (Auto-Encoding): เพิ่มการเรียกฟังก์ชัน create_encodings() ทันทีเมื่อมีการลงทะเบียนนักเรียนใหม่ หรือนำเข้าข้อมูลจาก Excel เพื่อให้ AI พร้อมใช้งานได้ทันทีโดยไม่ต้องรันสคริปต์แยก

แก้ปัญหาข้อมูลขยะใน AI: ปรับปรุงไฟล์ encode_faces.py ให้ตรวจสอบโฟลเดอร์รูปภาพอย่างเข้มงวด และสั่งลบไฟล์ encodings.pickle เดิมทิ้งก่อนสร้างใหม่ เพื่อป้องกันปัญหาการจำรหัสผิดพลาด (เช่น รหัส images_db)

อัปเกรดฐานข้อมูล (SQLAlchemy 2.0): เปลี่ยนการดึงข้อมูลจาก query.get() เป็น db.session.get() เพื่อให้เป็นไปตามมาตรฐานใหม่และเพิ่มประสิทธิภาพการทำงาน

ระบบความสัมพันธ์ข้อมูล (Relationship): เพิ่มการเชื่อมโยงข้อมูลระหว่างตารางนักเรียน (Student) และตารางการเข้าเรียน (Attendance) ทำให้ระบบสามารถดึงชื่อนักเรียนมาแสดงในหน้าจัดการข้อมูลได้ถูกต้อง

ระบบสรุปสถิติสำหรับกราฟ: เพิ่มลอจิกการคำนวณ today_stats ใน app.py เพื่อสรุปจำนวนคน มา, สาย, ขาด, ลา ของวันปัจจุบันส่งไปยังหน้า Dashboard

🎨 2. ส่วนติดต่อผู้ใช้งาน (UI/UX & Responsive)
โครงสร้าง Layout ใหม่ (Sidebar & Content): แยกส่วนเมนูข้าง (Sidebar) และเนื้อหาหลัก (Main Content) ออกจากกันชัดเจนโดยใช้ Flexbox ทำให้เนื้อหาหลักขยายเต็มพื้นที่เสมอและไม่โดนบดบัง

ระบบเมนูย่อ-ขยาย (Hamburger Menu): เพิ่มปุ่มสำหรับย่อ Sidebar ให้เหลือเพียงไอคอนเพื่อเพิ่มพื้นที่ใช้งาน พร้อมระบบจดจำสถานะล่าสุด (LocalStorage)

หน้าสแกนหน้า (Kiosk Interface):

ปรับส่วนเลือกรายวิชาเป็นระบบ Grid เพื่อไม่ให้กล่องตัวเลือกเบียดกันในหน้าจอขนาดเล็ก

เพิ่ม Viewfinder และ Scan Line Animation เพื่อให้ผู้ใช้จัดตำแหน่งใบหน้าได้ง่ายและดูเป็นมืออาชีพ

แดชบอร์ดสรุปสถิติแบบกราฟ (Visual Dashboard):

เพิ่ม Summary Cards แสดงตัวเลขสรุปสถานะวันนี้ (มา/สาย/ขาด) พร้อมแถบสีสื่อความหมาย

ติดตั้ง Doughnut Chart (กราฟวงกลม) เพื่อแสดงสัดส่วนการมาเรียนวันนี้แบบ Real-time

หน้าจัดการสถานะ (Attendance Management): ปรับปรุงหน้า attendance_data.html ให้แยกสัดส่วนนักเรียนที่สแกนหน้าแล้วและคนที่ยังไม่ได้เช็กชื่อออกจากกันอย่างชัดเจน


## 📝 รายละเอียดการอัปเดตล่าสุด (Change Log) - 25 Feb 2026

### 📊 ระบบวิเคราะห์ข้อมูล (Dashboard & Analytics)
- **Modern Dashboard Layout:** ปรับปรุงโครงสร้างหน้า Dashboard ใหม่ แบ่งสัดส่วน 70:30 เพื่อการแสดงผลที่มีประสิทธิภาพ
- **Dynamic Bar Chart:** เปลี่ยนจากกราฟวงกลมเป็นกราฟแท่งเปรียบเทียบสถิติแต่ละระดับชั้น (ม.1 - ม.6) แบบ Real-time
- **Class Ranking:** เพิ่มส่วนสรุป 5 อันดับห้องเรียนที่มีอัตราการเข้าเรียนสูงสุด
- **Navigation Tools:** เพิ่มปุ่ม **Back** และแก้ไขระบบ **Breadcrumb** ให้แม่นยำขึ้น

### 🔐 ระบบความปลอดภัยและการเข้าถึง (Security)
- **Teacher Login System:** เพิ่มระบบล็อกอินสำหรับคุณครูเพื่อจำกัดสิทธิ์การใช้งาน
- **Access Control:** นำ `@login_required` มาใช้ควบคุมหน้าสแกนและหน้าจัดการข้อมูลสำคัญ
- **Session Security:** กำหนด `secret_key` สำหรับป้องกันข้อมูล Session และรองรับระบบ Flash Messages

### 🐛 การแก้ไขข้อผิดพลาด (Bug Fixes)
- ลบส่วนแสดงผล Breadcrumb ที่ซ้ำซ้อน (แถบสีขาวว่าง) ออกจากหน้า Dashboard
- ปรับปรุงการจัดวางปุ่มย้อนกลับให้กึ่งกลางและสวยงามแบบ Modern UI
```
